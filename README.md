# NEURAL NETWORK (in progress)
#### README is still in progress

## Build library
``` sh
mkdir build
cd build
cmake ..
cd ..
cmake --build build
```

## Install library
``` sh
sudo cmake --install build
```